---
name: figma-code-bridge-parts-playbook
description: |
  Use this skill when Codex needs connect Figma design components to code components using Code Connect so design-system updates flow into the codebase automatically. Recast from the original figma-code-connect-components/SKILL.md material as the figma-code-bridge-parts-playbook procedure.
---

# Figma Code Bridge Parts Playbook

## Role

Use this skill when Codex needs connect Figma design components to code components using Code Connect so design-system updates flow into the codebase automatically. Recast from the original figma-code-connect-components/SKILL.md material as the figma-code-bridge-parts-playbook procedure.

## Source Trace

- Original Markdown: `figma-code-connect-components/SKILL.md`
- Reformulated skill name: `figma-code-bridge-parts-playbook`
- Upstream reference: https://github.com/figma/skills
- Source category: `figma`

## Operating Guidance

Follow the rewritten material below as the working procedure. Keep code blocks, commands, file paths, URLs, dimensions, and API names exact when applying the skill.

> Adapted from Figma's MCP server guide.

## Purpose

Connect Figma design components to code components using Code Connect so design-system updates flow into the codebase automatically.

## Origin

- Upstream: https://github.com/figma/skills
- Category: `figma`

## Procedure

This catalogue entry advertises the skill in Open Design so the agent
discovers it during planning. To execute the full upstream workflow with
its original assets, scripts, and references, install the upstream
bundle into your active agent's skills directory:

```bash
# Inspect the upstream README for exact paths
open https://github.com/figma/skills
```

Then request this skill by name (`figma-code-connect-components`) or with
one of the trigger phrases listed in this skill's frontmatter.
